class Solution:
    def multiply(self, num1: str, num2: str) -> str:
        '''subtracted= ord('0')
        lis1=[]
        lis2=[]
        for letter in num1:
            lis1.append( ord(letter)-subtracted)
        for letter in num2:
            lis2.append(ord(letter)-subtracted)
        for val in lis'''
        return str(eval(num1+'*'+num2))
        